## 1. Implementation

- [x] 1.1 Remove the `spec:none` provisioning row and retire the name

## 2. Verification

- [x] 2.1 `actionlint` passes on `pr-lint.yaml`
- [ ] 2.2 The next provisioning run deletes the live `spec:none` label
