name: Review Correctness

# Reusable body of the adjudicating correctness lane. Callers own the
# pull_request triggers, concurrency, and permissions; this body owns the
# logic and runs in the caller's repository with the caller's event.
on:
    workflow_call:
        secrets:
            RUNESEER_APP_ID:
                required: true
            RUNESEER_APP_KEY:
                required: true
            CLAUDE_CODE_OAUTH_TOKEN:
                required: true

# Single declaration point for the identities, labels, and names this
# body uses. Job-level if expressions cannot read env, so the guard
# keeps its literals; the reviewer prompt stays inline until the shared
# definitions file on the roadmap carries it.
env:
    LANE_BOTS: '["macroscopeapp[bot]", "cursor[bot]"]'
    APPS_EXPECTED: cursor
    APPS_EXPECTED_SUMMONED: cursor macroscopeapp
    LABELS_CONSUMED: "review review%3Amacroscope review%3Aruneseer"
    LABEL_RUNE_BLOCKED: "issue:rune"
    RUNESEER_PROVIDER_FAILURE_MESSAGE: |
        ### Runeseer is paused — review provider unavailable

        Runeseer could not complete this review. Claude usage may be exhausted, or the provider returned an error.

        An owner can restore Claude usage or wait for the limit to reset.

        Rerun the failed workflow only when the pull request head is unchanged. Otherwise, clear `issue:rune` after recovery and request a new round.
    LABEL_STAGE_CURSOR: "stage%3Acursor"
    LABEL_STAGE_MACROSCOPE: "stage%3Amacroscope"
    ARTIFACT_PREFIX: runeseer-verdict-pr
    INSTRUCTION_PATTERN: '(^|/)(CLAUDE|AGENTS)(\.local)?\.md$|^\.claude(/|$)|^\.cursor(/|$)|(^|/)copilot-instructions\.md$|^\.mcp\.json$|^\.rune$'
    POLL_SECONDS: 20
    POLL_ROUNDS: 15

jobs:
    correctness:
        name: correctness
        # Summoned, never ambient: the lane runs only while the owner's
        # review:runeseer label is on the pull request, on any non-draft
        # same-repository pull request — the owner controls spend by
        # applying the label, so contributor branches pass through the
        # same funnel. Fork pull requests cannot run this lane: GitHub
        # strips the secrets it needs from fork-triggered runs, and their
        # funnel ends at the free lanes plus the owner's review.
        if: >
            github.event.pull_request.draft == false &&
            github.event.pull_request.head.repo.full_name == github.repository &&
            contains(github.event.pull_request.labels.*.name, 'review:runeseer') &&
            (github.event.action != 'labeled' || startsWith(github.event.label.name, 'review'))
        runs-on: ubuntu-latest
        timeout-minutes: 40
        steps:
            # The head commit itself, not the synthetic merge ref: the verdict
            # must record the sha the approval step compares against.
            - uses: actions/checkout@08c6903cd8c0fde910a37f88322edcfb5dd907a8 # v5
              with:
                  ref: ${{ github.event.pull_request.head.sha }}
                  fetch-depth: 0
                  persist-credentials: false

            # The reviewer's instructions load from the base branch: a pull
            # request must not be able to rewrite the rules it is judged by.
            # Head-only instruction files are removed, not merely overlaid,
            # and nested CLAUDE.md/AGENTS.md count. The head's versions stay
            # reviewable through the diff.
            - name: Instructions from the base
              env:
                  BASE_SHA: ${{ github.event.pull_request.base.sha }}
              run: |
                  pattern=$INSTRUCTION_PATTERN
                  git ls-files -z | tr '\0' '\n' | grep -E "$pattern" | while IFS= read -r file; do
                      rm -f "$file"
                  done
                  rm -rf .claude .cursor
                  git ls-tree -r --name-only "$BASE_SHA" | grep -E "$pattern" | while IFS= read -r file; do
                      git checkout "$BASE_SHA" -- "$file" 2>/dev/null || true
                  done
                  echo "instruction surface pinned to the base branch."

            # Prose-only diffs are the conventions lane's territory; the
            # subscription reviews executable surface. Markdown under
            # docs/specs or .github stays in scope, since canon and
            # machinery are executable in the way that matters.
            - id: scope
              name: Scope to reviewable changes
              env:
                  GH_TOKEN: ${{ github.token }}
              run: |
                  set -o pipefail
                  # skip: stands the lane down before it spends anything, and
                  # the job still concludes success so the required mirror
                  # clears. A failed label read must not read as an applied skip.
                  labels=$(gh api --paginate "repos/${{ github.repository }}/issues/${{ github.event.pull_request.number }}/labels?per_page=100" \
                      --jq '[.[].name]' | jq -s -r 'add // [] | .[]') \
                      || { echo "could not read the live labels; rerun the check"; exit 1; }
                  if printf '%s\n' "$labels" | grep -qx 'skip:runeseer'; then
                      echo "skip=true" >> "$GITHUB_OUTPUT"
                      echo "skip:runeseer present; the correctness lane never runs."
                      exit 0
                  fi
                  files=$(gh pr diff ${{ github.event.pull_request.number }} --repo ${{ github.repository }} --name-only)
                  substantive=$(printf '%s\n' "$files" | grep -Ev '\.md$' || true)
                  in_scope_md=$(printf '%s\n' "$files" | grep -E '^(docs/specs/|\.github/).*\.md$' || true)
                  if [ -z "$substantive" ] && [ -z "$in_scope_md" ]; then
                      echo "skip=true" >> "$GITHUB_OUTPUT"
                      echo "Prose-only change; correctness lane stands down."
                  else
                      echo "skip=false" >> "$GITHUB_OUTPUT"
                  fi

            # The cascade summons this lane only after the free lanes settle,
            # so this wait is a short guard for directly requested rounds, not the
            # funnel's ordering mechanism.
            - name: Wait for the other lanes
              if: steps.scope.outputs.skip == 'false'
              env:
                  GH_TOKEN: ${{ github.token }}
                  HEAD_SHA: ${{ github.event.pull_request.head.sha }}
                  MACROSCOPE_SUMMONED: ${{ contains(github.event.pull_request.labels.*.name, 'review:macroscope') }}
              run: |
                  expected="$APPS_EXPECTED"
                  [ "$MACROSCOPE_SUMMONED" = "true" ] && expected="$APPS_EXPECTED_SUMMONED"
                  for _ in $(seq 1 "$POLL_ROUNDS"); do
                      settled=true
                      for app in $expected; do
                          runs=$(gh api --paginate "repos/${{ github.repository }}/commits/${HEAD_SHA}/check-runs?per_page=100" \
                              --jq "[.check_runs[] | select(.app.slug == \"$app\")]" | jq -s 'add // []')
                          total=$(printf '%s' "$runs" | jq 'length')
                          done_count=$(printf '%s' "$runs" | jq '[.[] | select(.status == "completed")] | length')
                          if [ "$total" = "0" ] || [ "$done_count" != "$total" ]; then
                              settled=false
                          fi
                      done
                      [ "$settled" = "true" ] && { echo "other lanes settled"; exit 0; }
                      sleep "$POLL_SECONDS"
                  done
                  echo "other lanes absent or still running past the poll budget; reviewing anyway"

            # Cross-round ledger: the previous round's verdict artifact, when
            # one exists, tells the adjudicator what was already reported and
            # which sha was last judged, enabling delta re-reviews.
            - id: ledger
              name: Fetch the previous verdict
              if: steps.scope.outputs.skip == 'false'
              env:
                  GH_TOKEN: ${{ github.token }}
                  BASE_SHA: ${{ github.event.pull_request.base.sha }}
              run: |
                  name="${ARTIFACT_PREFIX}${{ github.event.pull_request.number }}"
                  # Pages merge through jq -s before filtering, so a
                  # multi-page history still yields exactly one id; the
                  # runner's gh predates --slurp.
                  artifact=$(gh api --paginate "repos/${{ github.repository }}/actions/artifacts?name=$name&per_page=100" \
                      --jq '[.artifacts[] | select(.expired == false)]' \
                      | jq -s -r 'add // [] | sort_by(.created_at) | last | .id // empty')
                  prev_sha=""
                  prev_base=""
                  if [ -n "$artifact" ]; then
                      gh api "repos/${{ github.repository }}/actions/artifacts/$artifact/zip" > prev-verdict.zip \
                          && unzip -o prev-verdict.zip runeseer-verdict.json -d .previous-verdict >/dev/null \
                          && prev_sha=$(jq -r '.sha // empty' .previous-verdict/runeseer-verdict.json) \
                          && prev_base=$(jq -r '.base // empty' .previous-verdict/runeseer-verdict.json) || true
                  fi
                  # A ledger from a commit no longer in this history must not
                  # suppress re-reporting: after a rewrite, reintroduced
                  # defects are new defects.
                  if [ -n "$prev_sha" ] && ! git merge-base --is-ancestor "$prev_sha" HEAD 2>/dev/null; then
                      echo "ledger sha $prev_sha is not an ancestor of HEAD; discarding the ledger"
                      rm -rf .previous-verdict
                      prev_sha=""
                  fi
                  # A retarget or base advance changes the effective diff
                  # without a new head commit: the old ledger and the settled
                  # stages both judged a different change, so both reset.
                  if [ -n "$prev_sha" ] && [ "$prev_base" != "$BASE_SHA" ]; then
                      echo "ledger base ${prev_base:-unknown} is not the current base; discarding the ledger and the settled stages"
                      rm -rf .previous-verdict
                      prev_sha=""
                      for stage_label in $LABEL_STAGE_CURSOR $LABEL_STAGE_MACROSCOPE; do
                          gh api -X DELETE \
                              "repos/${{ github.repository }}/issues/${{ github.event.pull_request.number }}/labels/$stage_label" \
                              >/dev/null 2>&1 || true
                      done
                  fi
                  echo "prev_sha=${prev_sha}" >> "$GITHUB_OUTPUT"
                  echo "previous verdict: ${prev_sha:-none}"
                  # Each earlier round left one summary comment. Past the
                  # settled round count the lane stops opening new low-stakes
                  # findings, so a pull request cannot be held open by an
                  # endless supply of fresh nitpicks.
                  set -o pipefail
                  rounds=$(gh api --paginate "repos/${{ github.repository }}/issues/${{ github.event.pull_request.number }}/comments?per_page=100" \
                      --jq '[.[] | select(.user.login == "runeseer[bot]") | select(.body | startswith("review/correctness:"))] | length' \
                      | jq -s 'add // 0') \
                      || { echo "could not count earlier rounds; treating this as the first"; rounds=0; }
                  echo "rounds=${rounds}" >> "$GITHUB_OUTPUT"
                  echo "earlier rounds on this pull request: ${rounds}"

            # Only the lane bots' own words reach the model: any other
            # commenter's text is a prompt-injection vector into the
            # verdict that earns approval.
            - id: lanes
              name: Fetch the other lanes' findings
              if: steps.scope.outputs.skip == 'false'
              env:
                  GH_TOKEN: ${{ github.token }}
              run: |
                  mkdir -p .lane
                  lanes=$LANE_BOTS
                  gh api --paginate "repos/${{ github.repository }}/pulls/${{ github.event.pull_request.number }}/comments?per_page=100" \
                      --jq "[.[] | select(.user.login as \$login | $lanes | index(\$login))]" \
                      | jq -s 'add // []' > .lane/inline-comments.json
                  gh api --paginate "repos/${{ github.repository }}/issues/${{ github.event.pull_request.number }}/comments?per_page=100" \
                      --jq "[.[] | select(.user.login as \$login | $lanes | index(\$login))]" \
                      | jq -s 'add // []' > .lane/issue-comments.json
                  echo "inline: $(jq 'length' .lane/inline-comments.json), issue: $(jq 'length' .lane/issue-comments.json)"

            - id: runeseer
              if: steps.scope.outputs.skip == 'false'
              uses: actions/create-github-app-token@fee1f7d63c2ff003460e3d139729b119787bc349 # v2
              with:
                  app-id: ${{ secrets.RUNESEER_APP_ID }}
                  private-key: ${{ secrets.RUNESEER_APP_KEY }}

            - if: steps.scope.outputs.skip == 'false'
              uses: anthropics/claude-code-action@be7b93b1907a4abad570368f3c74b6fe3807510b # v1
              with:
                  claude_code_oauth_token: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
                  github_token: ${{ steps.runeseer.outputs.token }}
                  allowed_bots: "runewright"
                  # The progress comment runs wherever the action supports
                  # it; only labeled events, the review label, refuse the
                  # flag. The adjudicator runs on opus: the judging layer of
                  # the review cascade gets the strongest reviewer.
                  track_progress: ${{ github.event.action != 'labeled' }}
                  claude_args: |
                      --model opus
                      --allowedTools "Read,Write,Grep,Glob,mcp__github_inline_comment__create_inline_comment,Bash(gh pr comment:*),Bash(gh pr diff:*),Bash(gh pr view:*),Bash(git diff:*),Bash(git log:*),Bash(git rev-parse:*)"
                  prompt: |
                      You are runeseer, the adjudicating correctness reviewer. Write with the terse, factual register of Cursor Bugbot and Macroscope. Output rules below are binding.

                      Inputs:
                      - `.lane/inline-comments.json` contains inline findings from `macroscopeapp[bot]` and `cursor[bot]`.
                      - `.lane/issue-comments.json` contains issue-comment findings from those lanes.
                      - `.previous-verdict/runeseer-verdict.json` may contain the previous-round ledger.
                      - The previous verdict SHA is `${{ steps.ledger.outputs.prev_sha }}`.
                      - This pull request has already had `${{ steps.ledger.outputs.rounds }}` correctness rounds.

                      Convergence rule, binding:
                      - From the third round onward, open no new finding below High severity. Judge the other lanes' findings and recheck the ledger as usual, but a fresh nitpick of your own at that point holds the pull request open for no gain, and the author has already answered everything you raised.
                      - Report a novel High or Critical finding at any round: real defects always earn a report.
                      - A judgment you already made stands. Do not reverse an earlier round's accepted resolution because you now prefer a different trade-off; if an earlier decision looks wrong, say so once in the Digest as a note and leave the verdict clean.
                      - Never demand the opposite of what an earlier round demanded on the same code.

                      Review procedure:
                      - Judge every non-ledger finding from the other lanes against HEAD as `confirmed`, `disputed`, or `already addressed`.
                      - For `disputed`, state the concrete code fact that invalidates the finding. For `already addressed`, identify where HEAD fixes it.
                      - Never re-report a finding present in the previous ledger. Do not repeat it in an inline comment or the summary.
                      - Recheck ledger findings against HEAD. Carry each still-unresolved finding into the new verdict JSON; omit it only when HEAD addresses it.
                      - When the previous verdict SHA is an ancestor of HEAD, this is a re-round: review ONLY the range from that SHA to HEAD. Report only defects that range introduces or fails to fix. Never report a new finding whose lines that range did not change, however real it looks; a pre-existing condition on unchanged lines is out of scope and at most a `restart` reason.
                      - Review correctness only: logic errors, broken invariants, silent failure modes, security problems, and claims the diff does not uphold.
                      - Never duplicate another lane. A judgment in the summary is adjudication, not a second report.
                      - Post inline comments only for novel runeseer findings of Medium, High, or Critical severity. Put novel Low findings only in the summary Digest.

                      Inline comment format (the blank lines are part of the format; markdown folds adjacent lines into one paragraph):
                      ### <specific defect title, at most 8 words>

                      **<Critical|High|Medium> Severity**

                      <One paragraph of at most 2 sentences and 70 words. State the defect, its concrete failure consequence, and the primary `path:line` anchor.>

                      Evidence: <optional semicolon-separated `path:line` anchors>

                      Inline comment constraints:
                      - Anchor the comment to the most relevant reviewable changed line and include `path:line` in the body.
                      - Separate the title, severity, paragraph, and Evidence line with blank lines, or the renderer folds them together.
                      - Prefer one sentence. Use the second only when needed to connect cause and consequence.
                      - The optional Evidence line contains anchors only, with no argument or recap.
                      - Use at most one direct quotation per finding, limited to one source line. Prefer an anchor over a quotation.
                      - Do not post an inline comment for a finding already reported by another lane or the ledger.

                      Summary comment:
                      - Post exactly one summary with `gh pr comment`.
                      - Its first line must be exactly `review/correctness: clean @SHA` or `review/correctness: N findings @SHA`, using the full HEAD SHA and the total unresolved finding count.
                      - Then a blank line, then `#### Lane judgments`, then one bullet per non-ledger finding from another lane, exactly this shape:
                        `- **<lane>** <confirmed|disputed|already addressed> (`<path:line>`): <reason, one sentence, at most 25 words.>`
                      - Bold only the lane name. Wrap every path, label, identifier, and code term in backticks so it renders as a code chip. Never use pipes as separators and never emit tables.
                      - Keep each judgment to one bullet. Do not add paragraphs beneath it.
                      - Then a blank line, then `#### Digest`, then one bullet per novel Low runeseer finding:
                        `- `<path:line>`: <defect and consequence, one sentence, at most 25 words.>`
                      - If there are no novel Low findings, write `- None.`
                      - Do not repeat other-lane or ledger findings in Digest.
                      - When the verdict carries open findings from the ledger, add a blank line, then `#### Carried`, then one bullet per carried finding:
                        `- `<path:line>`: <its summary title>` — so every finding behind the count is visible in the summary. Omit the section when nothing is carried.

                      Verdict file:
                      - Write valid JSON to `runeseer-verdict.json` in the workspace root.
                      - Use exactly this schema:
                        `{"sha":"<full HEAD sha>","verdict":"clean|findings","count":N,"restart":"none|cursor|macroscope","findings":[{"path":"<path>","line":<line>,"summary":"<terse title>","lane":"<lane>","judgment":"<judgment>","comment_id":<optional originating comment database id>}],"lane_judgments":[{"path":"<path>","line":<line>,"summary":"<terse title>","lane":"<lane>","judgment":"confirmed|disputed|already addressed","reason":"<judgment sentence>","comment_id":<optional originating comment database id>}]}`
                      - Include every open genuine finding from any lane, including novel Low findings and open findings carried from the ledger.
                      - A finding is open only while the defect is still present at HEAD. Your Lane judgment decides: `confirmed` findings are open; `disputed` and `already addressed` findings are not, even when their review threads are still unresolved on GitHub, and a ledger entry whose defect the current HEAD fixes is not open either.
                      - When every lane finding is disputed or already addressed and no novel finding remains, the array is empty, `count` is 0, and the summary first line reads `review/correctness: clean @SHA`.
                      - Use `runeseer` as the lane for your own findings. Preserve the originating lane for other findings.
                      - Include every other-lane judgment in `lane_judgments`, including rechecked ledger findings and judgments excluded from `findings`.
                      - For non-ledger judgments, set `reason` to the same sentence used in the summary comment. For ledger judgments, write the same kind of short factual sentence.
                      - Copy `comment_id` from the originating entry's `id` in `.lane/inline-comments.json` or `.lane/issue-comments.json`. Omit it only when no originating comment exists.
                      - Preserve `comment_id` on carried ledger findings when it exists.
                      - Each `summary` is a factual title of at most 12 words.
                      - `count` must equal the findings array length. Set `verdict` to `clean` only when the array is empty; otherwise set it to `findings`.
                      - `restart` is almost always `none`. Set `cursor` (full funnel) or `macroscope` (macroscope onward) only when the accumulated change since the earlier stages settled is structurally large or shifts risk they never saw; state the reason as one Digest bullet.

                      Style prohibitions:
                      - No throat-clearing, narrative framing, rhetorical questions, rhetorical conclusions, praise, or review-process narration.
                      - No multi-paragraph finding, nested argument, diff recap, recommendation essay, or canon exegesis.
                      - Do not restate the diff. State the violated invariant and resulting failure once.
                      - Do not use block quotes.
                      - Do not write phrases such as "either way", "the comment stops short of", or "the PR does not hold together".
                      - Use present tense, concrete nouns, and direct causal language.

                      A review that posts nothing is a failed review. Even a clean review must write the verdict file and post the required summary.

            # The verdict binds to the base it was judged against, so a later
            # retarget invalidates the ledger. The lane writes this, not the
            # model, so it is never misremembered.
            - name: Bind verdict to base
              if: steps.scope.outputs.skip == 'false'
              env:
                  BASE_SHA: ${{ github.event.pull_request.base.sha }}
              run: |
                  [ -f runeseer-verdict.json ] || exit 0
                  jq --arg base "$BASE_SHA" '. + {base: $base}' runeseer-verdict.json > runeseer-verdict.bound.json
                  command mv runeseer-verdict.bound.json runeseer-verdict.json

            # Other-lane judgments close threads that no longer represent
            # defects. Confirmed findings stay open for the owner's decision.
            - name: Apply lane judgments
              if: steps.scope.outputs.skip == 'false'
              env:
                  GH_TOKEN: ${{ steps.runeseer.outputs.token }}
                  REPO: ${{ github.repository }}
                  PR: ${{ github.event.pull_request.number }}
                  HEAD_SHA: ${{ github.event.pull_request.head.sha }}
              run: |
                  set -o pipefail
                  [ -f runeseer-verdict.json ] || { echo "no verdict file; no lane judgments to apply"; exit 0; }
                  if ! jq -e '.lane_judgments | type == "array"' runeseer-verdict.json >/dev/null; then
                      echo "verdict file has no lane_judgments array; review threads remain unchanged"
                      exit 0
                  fi

                  thread_query='query($owner: String!, $repo: String!, $number: Int!, $endCursor: String) {
                      repository(owner: $owner, name: $repo) {
                          pullRequest(number: $number) {
                              reviewThreads(first: 100, after: $endCursor) {
                                  pageInfo { hasNextPage endCursor }
                                  nodes {
                                      id
                                      isResolved
                                      path
                                      line
                                      originalLine
                                      comments(first: 1) { nodes { databaseId author { login } } }
                                  }
                              }
                          }
                      }
                  }'
                  thread_query_failed=false
                  if ! threads=$(gh api graphql --paginate -f query="$thread_query" \
                      -f owner="${REPO%/*}" -f repo="${REPO#*/}" -F number="$PR" \
                      --jq '[.data.repository.pullRequest.reviewThreads.nodes[]]' \
                      | jq -s 'add // []'); then
                      echo "could not query review threads; the runeseer app token may lack pull-request read scope"
                      threads='[]'
                      thread_query_failed=true
                  fi

                  jq -c '.lane_judgments[] | select(.judgment == "disputed" or .judgment == "already addressed")' \
                      runeseer-verdict.json | while IFS= read -r judgment; do
                      path=$(jq -r '.path' <<<"$judgment")
                      line=$(jq -r '.line' <<<"$judgment")
                      reason=$(jq -r '.reason' <<<"$judgment")
                      comment_id=$(jq -r '.comment_id // empty' <<<"$judgment")
                      if [ "$thread_query_failed" = "true" ]; then
                          echo "could not resolve review thread for $path:$line because the thread query failed"
                          continue
                      fi
                      if [ -z "$comment_id" ]; then
                          echo "no originating inline comment id for $path:$line; leaving review threads unchanged"
                          continue
                      fi
                      thread=$(jq -c --arg comment_id "$comment_id" '
                          [.[] | select(
                              ((.comments.nodes[0].databaseId // "") | tostring) == $comment_id
                          )] | sort_by(.isResolved) | first // empty' <<<"$threads")
                      if [ -z "$thread" ]; then
                          echo "no review thread matched $path:$line${comment_id:+ (comment $comment_id)}"
                          continue
                      fi
                      if [ "$(jq -r '.isResolved' <<<"$thread")" = "true" ]; then
                          echo "review thread for $path:$line is already resolved; skipping"
                          continue
                      fi

                      first_comment_id=$(jq -r '.comments.nodes[0].databaseId // empty' <<<"$thread")
                      thread_id=$(jq -r '.id' <<<"$thread")
                      if [ -z "$first_comment_id" ]; then
                          echo "review thread for $path:$line has no reply target; leaving it unresolved"
                          continue
                      fi
                      if ! reply_error=$(gh api -X POST \
                          "repos/$REPO/pulls/$PR/comments/$first_comment_id/replies" \
                          -f body="$reason" 2>&1); then
                          echo "could not reply to review thread for $path:$line; leaving it unresolved: $reply_error"
                          continue
                      fi
                      if ! resolution=$(gh api graphql \
                          -f query='mutation($threadId: ID!) { resolveReviewThread(input: {threadId: $threadId}) { thread { isResolved } } }' \
                          -f threadId="$thread_id" 2>&1); then
                          echo "could not resolve review thread for $path:$line; the runeseer app token may lack pull-request write scope: $resolution"
                          continue
                      fi
                      if [ "$(jq -r '.data.resolveReviewThread.thread.isResolved // false' <<<"$resolution")" != "true" ]; then
                          echo "resolveReviewThread did not resolve $path:$line: $resolution"
                          continue
                      fi
                      echo "resolved review thread for $path:$line after posting its judgment"
                  done

                  # A clean verdict is the one statement that every finding this
                  # lane raised is gone, so it is the only safe moment to close
                  # the lane's own threads. Per-finding matching cannot do it:
                  # a novel runeseer finding has no originating comment to carry
                  # an id, so an open finding would look absent and its thread
                  # would close over a live defect. A verdict with any finding
                  # leaves every own thread alone.
                  if [ "$thread_query_failed" = "true" ]; then
                      echo "could not close this lane's own threads because the thread query failed"
                  elif ! jq -e '.verdict == "clean" and .count == 0
                      and (.findings | type == "array" and length == 0)' runeseer-verdict.json >/dev/null; then
                      echo "this head still carries findings; this lane's own threads stay open"
                  else
                      jq -c '.[] | select(.isResolved == false)
                          | select((.comments.nodes[0].author.login // "") == "runeseer[bot]")' <<<"$threads" \
                          | while IFS= read -r own_thread; do
                          own_comment_id=$(jq -r '.comments.nodes[0].databaseId // empty' <<<"$own_thread")
                          [ -n "$own_comment_id" ] || continue
                          own_path=$(jq -r '.path' <<<"$own_thread")
                          own_line=$(jq -r '.line // .originalLine // 0' <<<"$own_thread")
                          own_thread_id=$(jq -r '.id' <<<"$own_thread")
                          if ! own_reply=$(gh api -X POST \
                              "repos/$REPO/pulls/$PR/comments/$own_comment_id/replies" \
                              -f body="This finding is no longer present at $HEAD_SHA, which this lane judged clean." 2>&1); then
                              echo "could not reply to this lane's thread for $own_path:$own_line; leaving it unresolved: $own_reply"
                              continue
                          fi
                          if ! own_resolution=$(gh api graphql \
                              -f query='mutation($threadId: ID!) { resolveReviewThread(input: {threadId: $threadId}) { thread { isResolved } } }' \
                              -f threadId="$own_thread_id" 2>&1); then
                              echo "could not resolve this lane's thread for $own_path:$own_line: $own_resolution"
                              continue
                          fi
                          if [ "$(jq -r '.data.resolveReviewThread.thread.isResolved // false' <<<"$own_resolution")" != "true" ]; then
                              echo "resolveReviewThread did not resolve this lane's thread for $own_path:$own_line: $own_resolution"
                              continue
                          fi
                          echo "resolved this lane's own thread for $own_path:$own_line; this head judged clean"
                      done
                  fi

                  confirmed=$(jq -c '[.lane_judgments[] | select(.judgment == "confirmed")]' runeseer-verdict.json)
                  [ "$(jq 'length' <<<"$confirmed")" = "0" ] && exit 0
                  if ! prior_escalations=$(gh api --paginate \
                      "repos/$REPO/issues/$PR/comments?per_page=100" \
                      --jq '[.[] | select(.user.login == "runeseer[bot]") | .body]' \
                      | jq -s 'add // []'); then
                      echo "could not query prior owner escalations; skipping this escalation to prevent duplicate requests"
                      exit 0
                  fi
                  # An originating comment id is the only identity that both
                  # survives a reword and separates two findings, so it alone
                  # carries the dedup marker. A finding without one has no such
                  # identity: it escalates every round rather than borrowing a
                  # key that would silently drop it. The schema asks for the id
                  # on every lane finding, which keeps this path rare.
                  confirmed=$(jq -c --argjson prior "$prior_escalations" '
                      def escalation_key:
                          if .comment_id then ({lane, comment_id} | @base64) else null end;
                      map(. + {escalation_key: escalation_key})
                      | map(select(
                          .escalation_key as $key
                          | $key == null
                          or ($prior | any(.[]; contains("<!-- runeseer-escalation:\($key) -->")) | not)
                      ))' <<<"$confirmed")
                  [ "$(jq 'length' <<<"$confirmed")" = "0" ] && {
                      echo "all confirmed findings were already escalated"
                      exit 0
                  }
                  escalation=$(jq -r '
                      ["@N4M3Z, these confirmed findings need a human decision:", ""]
                      + (map("- `\(.path):\(.line)`: \(.summary)"
                          + (if .escalation_key then "\n<!-- runeseer-escalation:\(.escalation_key) -->" else "" end)))
                      + ["", "Please decide whether each finding should be fixed or accepted."]
                      | join("\n")' <<<"$confirmed")
                  if ! escalation_error=$(gh api -X POST "repos/$REPO/issues/$PR/comments" \
                      -f body="$escalation" 2>&1); then
                      echo "could not post the confirmed-finding escalation; the runeseer app token may lack issue-comment write scope: $escalation_error"
                  else
                      echo "posted one owner escalation for newly confirmed findings"
                  fi

            # The verdict artifact is the machine-readable record: the next
            # round's ledger, and the approval's evidence.
            - name: Upload verdict
              if: steps.scope.outputs.skip == 'false'
              uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v6
              with:
                  name: runeseer-verdict-pr${{ github.event.pull_request.number }}
                  path: runeseer-verdict.json
                  retention-days: 30
                  if-no-files-found: warn

            # Earned approval: a clean verdict FOR THIS HEAD, read from the
            # artifact rather than comment archaeology. The live head is
            # re-fetched first and the review is pinned to the judged commit,
            # so a push racing the verdict can never collect the approval.
            # A clean verdict that requests a restart approves nothing: the
            # restarted stages must settle before the merge gate opens.
            - name: Approve on clean verdict
              if: steps.scope.outputs.skip == 'false'
              env:
                  GH_TOKEN: ${{ steps.runeseer.outputs.token }}
                  HEAD_SHA: ${{ github.event.pull_request.head.sha }}
              run: |
                  [ -f runeseer-verdict.json ] || { echo "no verdict file; not approving"; exit 0; }
                  live=$(gh api "repos/${{ github.repository }}/pulls/${{ github.event.pull_request.number }}" --jq '.head.sha')
                  # Clean means explicitly, completely clean: every field
                  # present and exact, no defaults. A partial verdict object
                  # approves nothing.
                  if jq -e --arg sha "$HEAD_SHA" '
                      .verdict == "clean" and .count == 0
                      and (.findings | type == "array" and length == 0)
                      and .restart == "none"
                      and .sha == $sha' runeseer-verdict.json >/dev/null \
                      && [ "$live" = "$HEAD_SHA" ]; then
                      gh api -X POST "repos/${{ github.repository }}/pulls/${{ github.event.pull_request.number }}/reviews" \
                          -f event=APPROVE \
                          -f commit_id="$HEAD_SHA" \
                          -f body="Earned approval: review/correctness returned a clean verdict for ${HEAD_SHA}. Dismissed automatically on any new push."
                  fi

            # A review label requests one round, even when the scope stands down:
            # consuming the labels stops a later push from re-running the
            # paid lane out of funnel order. The next round starts with a
            # fresh review label.
            - name: Consume the review labels
              if: always()
              continue-on-error: true
              env:
                  GH_TOKEN: ${{ github.token }}
              run: |
                  for label in $LABELS_CONSUMED; do
                      gh api -X DELETE \
                          "repos/${{ github.repository }}/issues/${{ github.event.pull_request.number }}/labels/$label" \
                          >/dev/null 2>&1 || true
                  done
                  echo "review labels consumed; a new review label starts the next round."

            # The adjudicator's requested restart clears the recorded stage
            # labels, so the next round re-runs the named stage onward. The
            # workflow token's label events trigger nothing. The clears are
            # verified: a stage label that survives would silently skip a
            # stage the adjudicator wanted re-run, so this step fails loudly.
            - name: Apply requested restart
              if: steps.scope.outputs.skip == 'false'
              env:
                  GH_TOKEN: ${{ github.token }}
              run: |
                  [ -f runeseer-verdict.json ] || exit 0
                  restart=$(jq -r '.restart // "none"' runeseer-verdict.json)
                  clear_stage() {
                      gh api -X DELETE \
                          "repos/${{ github.repository }}/issues/${{ github.event.pull_request.number }}/labels/$1" \
                          >/dev/null 2>&1 || true
                      remaining=$(gh api --paginate \
                          "repos/${{ github.repository }}/issues/${{ github.event.pull_request.number }}/labels?per_page=100" \
                          --jq '[.[].name]' | jq -s --arg label "$(printf '%s' "$1" | sed 's/%3A/:/')" \
                          'add // [] | index($label) != null')
                      [ "$remaining" = "false" ] \
                          || { echo "could not clear stage label $1; the restart did not apply"; exit 1; }
                  }
                  case "$restart" in
                      cursor)
                          clear_stage "$LABEL_STAGE_CURSOR"
                          clear_stage "$LABEL_STAGE_MACROSCOPE"
                          echo "restart requested: the next round re-runs the full funnel."
                          ;;
                      macroscope)
                          clear_stage "$LABEL_STAGE_MACROSCOPE"
                          echo "restart requested: the next round re-runs macroscope onward."
                          ;;
                      none)
                          echo "no restart requested."
                          ;;
                      *)
                          echo "unrecognized restart value: $restart"
                          exit 1
                          ;;
                  esac

            # A round that produced no verdict is a provider failure, not a
            # finding: the blocker label stops the cascade from re-summoning
            # this lane until the problem is fixed. A round that produced a
            # verdict clears the blocker. The workflow token's label events
            # trigger nothing, so neither write re-enters the cascade.
            # A cancelled round (superseded by a push or new review label) is not
            # a provider failure and must not trip the breaker.
            - name: Provider circuit breaker
              if: always() && !cancelled() && steps.scope.outputs.skip == 'false'
              continue-on-error: true
              env:
                  GH_TOKEN: ${{ github.token }}
                  HEAD_SHA: ${{ github.event.pull_request.head.sha }}
                  RUNESEER_TOKEN: ${{ steps.runeseer.outputs.token }}
              run: |
                  set -o pipefail
                  encoded=$(printf '%s' "$LABEL_RUNE_BLOCKED" | sed 's/:/%3A/')
                  # Only a schema-complete verdict proves the provider worked;
                  # a partial file from a mid-review crash keeps the blocker.
                  if [ -f runeseer-verdict.json ] && jq -e '
                      (.sha | type == "string" and length > 0)
                      and (.verdict == "clean" or .verdict == "findings")
                      and (.count | type == "number")
                      and (.restart == "none" or .restart == "cursor" or .restart == "macroscope")
                      and (.findings | type == "array")
                      and (.lane_judgments | type == "array")' runeseer-verdict.json >/dev/null 2>&1; then
                      gh api -X DELETE \
                          "repos/${{ github.repository }}/issues/${{ github.event.pull_request.number }}/labels/$encoded" \
                          >/dev/null 2>&1 || true
                  else
                      gh api "repos/${{ github.repository }}/labels/$encoded" >/dev/null 2>&1 \
                          || gh api -X POST "repos/${{ github.repository }}/labels" \
                              -f name="$LABEL_RUNE_BLOCKED" -f color="d93f0b" \
                              -f description="Review provider requires intervention" >/dev/null
                      gh api -X POST \
                          "repos/${{ github.repository }}/issues/${{ github.event.pull_request.number }}/labels" \
                          -f "labels[]=$LABEL_RUNE_BLOCKED" >/dev/null
                      if [ -z "$RUNESEER_TOKEN" ]; then
                          echo "::warning::Runeseer app token is unavailable. The provider failure notice was not posted"
                          exit 0
                      fi

                      # A fixed message explains the failure without copying
                      # provider output or pull request text into the comment.
                      marker="<!-- runeseer-provider-failure:$HEAD_SHA -->"
                      if ! comments=$(gh api --paginate \
                          "repos/${{ github.repository }}/issues/${{ github.event.pull_request.number }}/comments?per_page=100" \
                          --jq '[.[] | select(.user.login == "runeseer[bot]") | .body]' \
                          | jq -s 'add // []'); then
                          echo "::warning::could not read Runeseer comments. The provider failure notice was not posted"
                          exit 0
                      fi
                      if jq -e --arg marker "$marker" 'any(.[]; contains($marker))' <<<"$comments" >/dev/null; then
                          echo "the provider failure notice already exists for $HEAD_SHA"
                          exit 0
                      fi

                      run_url="${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
                      if ! job_url=$(gh api --paginate \
                          "repos/${{ github.repository }}/actions/runs/${{ github.run_id }}/jobs?per_page=100" \
                          --jq '[.jobs[] | select(.name == "review / correctness")]' \
                          | jq -s -r 'add // [] | first // empty | .html_url // ""'); then
                          job_url=""
                      fi
                      [ -n "$job_url" ] || job_url="$run_url"
                      body=$(printf '%s\n\n[Open the failed workflow](%s)\n\n%s' \
                          "$RUNESEER_PROVIDER_FAILURE_MESSAGE" "$job_url" "$marker")
                      if ! GH_TOKEN="$RUNESEER_TOKEN" gh api -X POST \
                          "repos/${{ github.repository }}/issues/${{ github.event.pull_request.number }}/comments" \
                          -f body="$body" >/dev/null; then
                          echo "::warning::could not post the Runeseer provider failure notice"
                      else
                          echo "posted one provider failure notice for $HEAD_SHA"
                      fi
                  fi

            # The spec's word is binding: a lane holding unresolved findings
            # fails its check. Green means clean for THIS head, never merely
            # finished.
            - name: Verdict gates the check
              if: steps.scope.outputs.skip == 'false'
              env:
                  GH_TOKEN: ${{ github.token }}
                  HEAD_SHA: ${{ github.event.pull_request.head.sha }}
              run: |
                  set -o pipefail
                  [ -f runeseer-verdict.json ] || { echo "no verdict file; the review did not complete"; exit 1; }
                  if jq -e --arg sha "$HEAD_SHA" '
                      .verdict == "clean" and .count == 0
                      and (.findings | type == "array" and length == 0)
                      and .restart == "none"
                      and .sha == $sha' runeseer-verdict.json >/dev/null; then
                      exit 0
                  fi
                  # The verdict, its findings, and its inline comments all
                  # stand: ignore: withdraws only their power to hold the
                  # merge, and the record stays on the pull request. A failed
                  # label read must not read as an applied ignore.
                  labels=$(gh api --paginate "repos/${{ github.repository }}/issues/${{ github.event.pull_request.number }}/labels?per_page=100" \
                      --jq '[.[].name]' | jq -s -r 'add // [] | .[]') \
                      || { echo "could not read the live labels; rerun the check"; exit 1; }
                  if printf '%s\n' "$labels" | grep -qx 'ignore:runeseer'; then
                      echo "ignore:runeseer present; the findings below stand as a record and no longer hold the merge."
                      jq -c '{verdict, count, sha, restart}' runeseer-verdict.json || true
                      exit 0
                  fi
                  echo "the verdict is not an explicit no-restart clean for this head; the check stays red."
                  jq -c '{verdict, count, sha, restart}' runeseer-verdict.json || true
                  exit 1

            # Metrics: cost, turns, and finding counts land in the job
            # summary so the round-trip's worth stays measured, not vibed.
            - name: Metrics
              if: always() && steps.scope.outputs.skip == 'false'
              run: |
                  exec_log=/home/runner/work/_temp/claude-execution-output.json
                  cost="?"; turns="?"
                  if [ -f "$exec_log" ]; then
                      cost=$(jq -r '[.[] | select(.type == "result")] | last | .total_cost_usd // "?"' "$exec_log" 2>/dev/null || echo "?")
                      turns=$(jq -r '[.[] | select(.type == "result")] | last | .num_turns // "?"' "$exec_log" 2>/dev/null || echo "?")
                  fi
                  count="?"
                  [ -f runeseer-verdict.json ] && count=$(jq -r '.count // "?"' runeseer-verdict.json)
                  {
                      echo "### review/correctness metrics"
                      echo "- unresolved findings: $count"
                      echo "- turns: $turns, est. cost: \$$cost"
                      echo "- previous verdict sha: ${{ steps.ledger.outputs.prev_sha || 'none' }}"
                  } >> "$GITHUB_STEP_SUMMARY"
